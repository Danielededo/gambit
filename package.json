{
  "name": "gambit",
  "version": "0.6.0",
  "description": "Chess in the browser: local play against Stockfish, or online games via PIN.",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "start": "node server/server.js",
    "test": "node test/protocol.test.mjs && node test/security.test.mjs && node test/social.test.mjs && node test/sound.test.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "ws": "^8.18.0"
  }
}
